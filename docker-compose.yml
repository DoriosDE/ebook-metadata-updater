services:
  ebook-metadata-updater:
    build: .
    volumes:
      - .:/ebooks
    environment:
      DIRECTORY: "/ebooks"
      TEMPLATE: "{author} {type} {year} - Ausgabe {ausgabe} ({year}-{month}-{day})"
      TITLE: "{ausgabe}/{year[-2:]}"
      DESCRIPTION: "{author} {type} {ausgabe}/{year[-2:]}"
      SUBJECT: "{author} {type}"
      LOG_AVAILABLE: false